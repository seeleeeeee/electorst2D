#include "geometryparser.h"

electrostatic::Geometry GeometryParser::parseString(const std::string& jsonText) const {
    return electrostatic::Geometry();
}

electrostatic::Geometry GeometryParser::parseFile(const std::string& filePath) const {
    return electrostatic::Geometry();
}